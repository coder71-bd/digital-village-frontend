import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
  botName: 'Digital Village ',
  initialMessages: [createChatBotMessage(`How can i help you?`)],
};

export default config;
